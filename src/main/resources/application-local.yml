logging:
  level:
    org:
      hibernate:
        type:
          descriptor:
            sql:
              BasicBinder: trace

spring:
  datasource:
    url: "jdbc:mysql://127.0.01:3306/instagram"
    username: "root"
    password: "password"
    driverClassName: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
